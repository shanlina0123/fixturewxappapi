<!--pages/inviteuser/makesure.wxml-->
<view class='padding'>
    <view class='joblist'>
        <text wx:for="{{data}}" wx:key="index" class='jobitem {{index==isadd?"on":""}}'  data-index="{{index}}"  data-id="{{item.id}}" bindtap='addClass'>{{item.name}}</text>
    </view>
    <button bindtap='codeUrl' class='surebtn'>确认</button>
</view>